# Ideas gatherer client environment

## Install packages
`$ yarn`

## To run the client:
`$ yarn start`

## To run the server:
`$ yarn server`

## To run the tests:
`$ yarn server # start the server before`
`$ yarn test`